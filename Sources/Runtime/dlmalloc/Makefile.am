##   A slightly-modified version of Doug Lea's malloc which is used
##   (interfacing with the PAL's Memory Manager) as the kernel's default
##   allocator - upstream source available from <https://gee.cs.oswego.edu/dl/>

EXTRA_DIST = malloc.h malloc.c
